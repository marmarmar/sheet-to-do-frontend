import { TasksTitleFilterPipe } from './tasks-title-filter.pipe';

describe('TasksTitleFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new TasksTitleFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
